Run `python -m features.ultimate_150_features` (or call `test_ultimate_features()`) to execute the full 150+ pipeline; requires `data/xauusd_m5.csv` plus optional `data/dxy_daily.csv`, `spx_daily.csv`, `us10y_daily.csv`, `vix_daily.csv`, `oil_wti_daily.csv`, `bitcoin_daily.csv`, `eurusd_daily.csv`, `silver_daily.csv`, `gld_etf_daily.csv`, and `data/economic_events_2015_2025.json`. Missing files are logged as warnings and filled with zeros rather than failing.